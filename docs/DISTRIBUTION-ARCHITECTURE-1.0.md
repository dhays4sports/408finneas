# 408FARMERS distribution candidate

408FARMERS remains the acquisition brand and campaign doorway. CoverageFit owns canonical evidence and Signal decisions. The domain, repository and existing public campaign URLs remain active.

The unlinked `/buyer/continue.html` candidate is a thin POST handoff to CoverageFit `/check-in/`. It reuses `CoverageFitLauncher.getAttribution`, an allowlisted projection and a single endpoint in `shared/config.js`. No local question/priority engine is loaded. It is noindex and absent from public navigation/sitemap. `/buyer/` is unchanged until hosted producer-receipt certification.

The complete route inventory, migration matrix, duplicate audit, handoff/attribution contract, specialized exceptions, SEO/deprecation plan and QA gates are canonical in CoverageFit:

- `docs/DISTRIBUTION-ROUTE-INVENTORY-1.0.md`
- `docs/DISTRIBUTION-ARCHITECTURE-1.0.md`

Life application/queue/conversion, commercial, local/referral, legacy appointment and thank-you dependencies remain intact. No redirects, route deletions, Cloudflare changes, migrations, SMS sends or campaign changes are part of this candidate.

Candidate activation requires CoverageFit endpoint deployment first, then safe cross-domain, ZERO-REPEAT, explicit permission and single producer-record verification. Keep current `/buyer/` available for rollback and existing appointment sessions. Do not migrate the remaining routes simply because their context appears in a registry.
