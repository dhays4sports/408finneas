# Remaining affinity staging

Teachers, Healthcare and Engineers legacy copies, exact alias dispatch and canonical compatibility destinations are prepared on a separate branch. No active-set expansion. CoverageFit retains question selection and evidence; affinity is attribution only. Activate one route after its predecessor hosted gate, add that route directory to Pages routing, then certify first answer/resume/receipt before the next. Preserve original index and legacy links. Rollback only the newly active route. No schema, scoring, CRM, SMS or Cloudflare configuration change.
