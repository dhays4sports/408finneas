(function(){'use strict';const form=document.getElementById('drawingEntryForm');if(!form)return;const params=new URLSearchParams(location.search);['utm_source','utm_medium','utm_campaign','utm_content'].forEach(k=>{const el=form.elements[k];if(el)el.value=params.get(k)||''});form.elements.landing_page.value=location.href;form.elements.submitted_at.value=new Date().toISOString();form.addEventListener('submit',async e=>{e.preventDefault();const error=form.querySelector('.form-error');error.hidden=true;if(!form.reportValidity())return;const data=new FormData(form);if(!String(data.get('mobile')||'').trim()&&!String(data.get('email')||'').trim()){error.textContent='Enter a mobile number or email so Dylan can notify the winner.';error.hidden=false;return}const button=form.querySelector('button[type=submit]');button.disabled=true;button.textContent='Submitting...';try{const response=await fetch(form.dataset.proxyEndpoint,{method:'POST',body:data,headers:{Accept:'application/json'}});if(!response.ok)throw new Error('submit');form.hidden=true;const success=document.getElementById('drawingSuccess');success.hidden=false;success.focus()}catch(_){error.textContent='Your entry could not be submitted. Please try again.';error.hidden=false;button.disabled=false;button.textContent='Enter the monthly drawing'}})})();
